{
  "name": "@semanticintent/rune-cli",
  "version": "1.2.0",
  "description": "CLI tooling for Rune Protocol — validate binding manifests",
  "bin": {
    "rune": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "node --test dist/test"
  },
  "files": [
    "dist",
    "schema"
  ],
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
